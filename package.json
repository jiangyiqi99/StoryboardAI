{
  "name": "storyboard-ai-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-native local non-linear video editor scaffold built with Electron, React, TypeScript, and Vite.",
  "author": "StoryboardAI",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "tsc --noEmit && electron-vite build",
    "dist": "npm run native:libav && npm run build && electron-builder",
    "dist:mac": "npm run native:libav && npm run build && electron-builder --mac --universal",
    "dist:win": "npm run native:libav && npm run build && electron-builder --win",
    "dist:linux": "npm run native:libav && npm run build && electron-builder --linux",
    "preview": "electron-vite preview",
    "typecheck": "tsc --noEmit",
    "native:libav": "node native/libav/scripts/build.mjs && node native/shared-memory-bridge/scripts/build.mjs",
    "native:bridge": "node native/shared-memory-bridge/scripts/build.mjs"
  },
  "build": {
    "appId": "ai.storyboard.editor",
    "productName": "Storyboard AI Editor",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "out/**",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "native/libav/bin",
        "to": "native/libav/bin"
      },
      {
        "from": "native/shared-memory-bridge/bin",
        "to": "native/shared-memory-bridge/bin"
      }
    ],
    "mac": {
      "category": "public.app-category.video",
      "target": [
        "dmg",
        "zip"
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "linux": {
      "category": "AudioVideo",
      "target": [
        "AppImage",
        "deb"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "electron": "^37.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^3.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
